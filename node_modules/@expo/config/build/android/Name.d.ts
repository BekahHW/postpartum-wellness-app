import { ExpoConfig } from '../Config.types';
export declare function getName(config: ExpoConfig): string | null;
/**
 * Changes the display name on the home screen,
 * notifications, and others.
 */
export declare function setName(configOrName: ExpoConfig | string, projectDirectory: string): Promise<boolean>;
