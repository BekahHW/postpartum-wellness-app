import { ExpoConfig } from '../Config.types';
import { Document } from './Manifest';
export declare function getAllowBackup(config: Pick<ExpoConfig, 'android'>): boolean;
export declare function setAllowBackup(config: Pick<ExpoConfig, 'android'>, manifestDocument: Document): Promise<Document>;
export declare function getAllowBackupFromManifest(manifestDocument: Document): boolean | null;
